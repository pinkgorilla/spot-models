{
  "name": "spot-models",
  "version": "1.0.0",
  "description": "spot models",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/index.js",
    "cover": "istanbul cover _mocha",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls",
    "codacy": "npm run cover -- --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/.bin/codacy-coverage && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pinkgorilla/spot-models.git"
  },
  "author": "Tris Setiawan",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/pinkgorilla/spot-models/issues"
  },
  "homepage": "https://github.com/pinkgorilla/spot-models#readme",
  "dependencies": { 
    "should": "^11.1.1"
  },
  "devDependencies": {
    "coveralls": "^2.11.15",
    "codacy-coverage": "^2.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "mocha-lcov-reporter": "^1.2.0"
  }
}
